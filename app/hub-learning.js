(function(){
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function slug(s){return String(s||'mtech-course').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}
function download(name,type,text){const b=new Blob([text],{type});const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=name;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},500)}
function courseText(path,mods){return `M-TECH SKILLS & LEARNING CENTER\n${path.title}\n${path.description||''}\n\n`+mods.map((m,i)=>`LESSON ${i+1}: ${m.title}\n\n${m.content||''}`).join('\n\n')+`\n\nM-TECH — WITH GOD WE BUILD SOLUTIONS\nTechnology is our passion, Education is our life…`}
function expandedText(path,mods){const base=courseText(path,mods);const sections=['Learning Objectives','Core Concepts','Practical Application','Worked Example','Common Problems and Troubleshooting','Best Practices','Hands-on Exercise','Review Questions','Key Terms','Summary and Next Steps'];return sections.map((h,i)=>`PAGE ${i+1} — ${h}\n\n${base}\n\n${h}: Study the course concepts carefully, apply them to a practical M-TECH or real-world scenario, record what you observed, explain why the result occurred, and identify one improvement you would make. Learners should be able to describe the concept in their own words and demonstrate it safely and responsibly.\n`).join('\n\n');}
function htmlDoc(path,mods){const text=expandedText(path,mods);return `<!doctype html><html><head><meta charset="utf-8"><title>${esc(path.title)}</title><style>body{font-family:Arial,sans-serif;max-width:850px;margin:auto;padding:40px;line-height:1.65}h1{margin-bottom:4px}.brand{font-weight:800}.page{page-break-after:always;white-space:pre-wrap}footer{margin-top:30px;border-top:1px solid #aaa;padding-top:10px}</style></head><body><div class="brand">M-TECH SKILLS & LEARNING CENTER</div><h1>${esc(path.title)}</h1><p>${esc(path.description||'')}</p>${text.split(/PAGE \d+ — /).filter(Boolean).map(x=>`<section class="page">${esc(x)}</section>`).join('')}<footer>WITH GOD WE BUILD SOLUTIONS · Technology is our passion, Education is our life…</footer></body></html>`}
function wordDoc(path,mods){return `<!doctype html><html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word"><head><meta charset="utf-8"><title>${esc(path.title)}</title></head><body>${htmlDoc(path,mods).replace(/^.*?<body>/s,'').replace(/<\/body>.*$/s,'')}</body></html>`}
async function getCourse(id){const [{data:path},{data:mods,error}]=await Promise.all([SB.from('hub_learning_paths').select('*').eq('id',id).maybeSingle(),SB.from('hub_learning_modules').select('*').eq('path_id',id).order('sort_order',{ascending:true})]);if(error)throw error;return {path,mods:mods||[]}}
async function renderLearning(){if(typeof SB==='undefined')return;const box=document.getElementById('hubLearning');if(!box)return;const {data:paths,error}=await SB.from('hub_learning_paths').select('*').eq('active',true).order('sort_order',{ascending:true});if(error){box.innerHTML='<p class="muted">Learning Center could not load.</p>';return}if(!paths?.length){box.innerHTML='<p class="muted">No learning paths yet.</p>';return}box.innerHTML=paths.map(p=>`<div class="notice" style="margin-bottom:10px"><div style="display:flex;justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap"><div><b>${esc(p.title)}</b><br><span class="muted">${esc(p.category||'Learning')} · ${esc(p.level||'All Levels')}</span><br><span class="muted">${esc(p.description||'')}</span><br><small class="muted">10-page extended study-note edition available</small></div><button class="btn alt small" data-open-course="${p.id}">Open Course / View Notes</button></div></div>`).join('');box.querySelectorAll('[data-open-course]').forEach(b=>b.onclick=()=>openCourse(b.dataset.openCourse))}
async function openCourse(id){let c;try{c=await getCourse(id)}catch(err){return alert(err.message)}const {path,mods}=c;const body=`<div class="notice"><b>${esc(path?.title||'Course')}</b><br><span class="muted">${esc(path?.description||'')}</span><br><small>Extended 10-page study-note edition</small></div><div class="actions" style="margin:12px 0;flex-wrap:wrap"><button class="btn small" onclick="mtechCourseDownload('${id}','pdf')">Save / Print PDF</button><button class="btn alt small" onclick="mtechCourseDownload('${id}','doc')">Download Word</button><button class="btn alt small" onclick="mtechCourseDownload('${id}','html')">Download HTML</button><button class="btn alt small" onclick="mtechCourseDownload('${id}','txt')">Download TXT</button><button class="btn alt small" onclick="mtechCourseDownload('${id}','md')">Download Markdown</button><button class="btn alt small" onclick="mtechCourseDownload('${id}','share')">Share / Save</button></div><p class="muted">PDF uses your device print/save-to-PDF service. Word downloads as a Word-compatible .doc file. Other formats can be saved offline and shared.</p><div>${mods.length?mods.map((m,i)=>`<details class="notice" ${i===0?'open':''} style="margin-bottom:10px"><summary style="cursor:pointer;font-weight:800">Lesson ${i+1}: ${esc(m.title)}</summary><div style="padding-top:10px;line-height:1.65;white-space:pre-wrap">${esc(m.content||'No notes yet.')}</div></details>`).join(''):'<p class="muted">No lessons have been added to this course yet.</p>'}</div>`;if(typeof modal==='function')modal('Skills & Learning Center — '+(path?.title||'Course'),body);else alert((path?.title||'Course')+'\n\n'+mods.map(m=>m.title+'\n'+m.content).join('\n\n'))}
window.mtechCourseDownload=async(id,fmt)=>{let c;try{c=await getCourse(id)}catch(err){return alert(err.message)}const {path,mods}=c,name=slug(path.title),text=expandedText(path,mods),html=htmlDoc(path,mods);if(fmt==='txt')return download(name+'-10-page-notes.txt','text/plain;charset=utf-8',text);if(fmt==='md')return download(name+'-10-page-notes.md','text/markdown;charset=utf-8','# '+path.title+'\n\n'+text.replace(/PAGE (\d+) — (.*)/g,'## Page $1 — $2'));if(fmt==='html')return download(name+'-10-page-notes.html','text/html;charset=utf-8',html);if(fmt==='doc')return download(name+'-10-page-notes.doc','application/msword',wordDoc(path,mods));if(fmt==='share'){if(navigator.share){const f=new File([text],name+'-10-page-notes.txt',{type:'text/plain'});try{return await navigator.share({title:path.title,text:'M-TECH Skills & Learning Center notes',files:navigator.canShare?.({files:[f]})?[f]:undefined})}catch(_){return}}return download(name+'-10-page-notes.txt','text/plain;charset=utf-8',text)}if(fmt==='pdf'){const w=window.open('','_blank');if(!w)return alert('Please allow pop-ups so the PDF print/save page can open.');w.document.write(html);w.document.close();setTimeout(()=>w.print(),350)}};
window.mtechOpenCourse=openCourse;const observer=new MutationObserver(()=>{const box=document.getElementById('hubLearning');if(box&&!box.dataset.learningReady){box.dataset.learningReady='1';renderLearning()}});observer.observe(document.documentElement,{childList:true,subtree:true});setInterval(()=>{const hub=document.getElementById('hub'),box=document.getElementById('hubLearning');if(hub?.classList.contains('active')&&box&&!box.querySelector('[data-open-course]'))renderLearning()},1500);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',renderLearning);else setTimeout(renderLearning,300);
})();